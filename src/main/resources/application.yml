server:
  address: 0.0.0.0      # ????????????????
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/attendance?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath*:mappers/*.xml
  type-aliases-package: caijing.com.attendance.domain

# ????/??
logging:
  level:
    root: INFO
